%h1 Dungeons

- if @dungeons.empty?
  %p Nothing here.
- else
  - @dungeons.in_groups_of(3, false) do |group|
    .row.dungeons-row
      - group.each do |dungeon|
        .col-md-4
          %a{target: :_blank, href: dungeon_path(dungeon)}
            = image_tag dungeon.image.thumb('300x200').url